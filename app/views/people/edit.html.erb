<div class="card border-primary">
  <div class="card-header text-center">
    <h3><b class="titulos_admin">EDITAR PERFIL DE: <%= @person.name.upcase %></b></h3>
    <div class="row">
      <div class="col-md-10">
      </div>
      <div class="col-md-2">
        <%= link_to 'Regresar', people_path, class: "btn btn-warning float-right" %>
      </div>
    </div>
  </div>
  <div class="card-body">
    <h4><b>Modifique los campos deseados del <%= @person.name %>:</b></h4>
    <%= render 'form', person: @person %>
  </div>
</div>
